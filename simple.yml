name: shell commamnds

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: eccho a string
        run: echo " my name is sasank "
      
      - name: multiline script
        run: |
          node -v
          npm -vgit